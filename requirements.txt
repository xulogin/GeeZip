# GeeZip 的 Python 依赖。装它们：
#     pip install -r requirements.txt
#
# 注意：不需要 torch / tensorflow。编码器和解码器是 numpy 手写的，
# 小到不值得上框架（编码器通常几百到几千个参数）。

numpy>=1.20
pandas>=1.2
scikit-learn>=1.0
earthengine-api>=0.1.380
rasterio>=1.2
scikit-image>=0.18
matplotlib>=3.4
