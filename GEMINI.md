# GeeZip

**完整说明在同目录的 `AGENTS.md`，请先读那一份再动手。**

那份写清了这套东西是什么、六条铁律、常用命令和目录地图，读完就能开工。

---

先记住最要紧的三条，万一没读到 `AGENTS.md` 也别犯错：

1. **先判断该不该用 GeeZip。** 写普通 GEE 脚本用 **GeeLo**；跑神经网络、要
   逐像元 σ 用 **GeeDL**。**只有"下载量是瓶颈"才用 GeeZip** —— 小数据直接下更快。
2. **两道核对都过了才许下载。** 结论是脚本自己判的（`GATE_RESULT PASS/FAIL`），
   没过 `python run.py 下载` 会直接拒绝执行。**不要想办法绕过。**
3. **「跑通」不等于「对」** —— GEE 惰性计算，零报错也可能结果全错。
   跑完还要看数值合不合理、量级对不对。压缩比按**实际传输字节**算、
   基线用 **uint16 不是 float32**；换地方退化了**照实说**。

只有一个入口：`python run.py`（不带参数看帮助）。

**给人看的入口**在 `README.md`。
