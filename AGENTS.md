# GeeZip · 给 AI 的完整说明

**给人看的入口在 `README.md`。这一份是给要动手跑它的 AI 看的。**

---

## 这套东西是什么

下遥感数据最花时间的一步，是把几十个波段原样搬回本地。而很多人下回来第一件事
就是降维——占带宽的那一步白走了。

GeeZip 把顺序倒过来：

```
云端（GEE）              带宽            本地
特征栈 D 维  →  编码器  →  k 维 uint8  →  解码器  →  还原 D 维
                前向传播                  反向传播在这边训
```

编码器和解码器在本地一起训，只有编码器搬上 GEE。
GEE 能前向传播，做不了反向传播，所以训练只能在本地；
解码本来就发生在下载之后，留本地天经地义。

实测：6 波段少下 4.2 倍，24 维时序少下 8.1 倍，
还原误差 0.0043 / 0.0091 反射率（Landsat 自身定标精度约 0.005）。

---

## ★ 先判断该不该用

| 用户要什么 | 用什么 |
|---|---|
| 写 NDVI / RSEI / LST 脚本、调 GEE 报错 | **geelo**，不是这个 |
| 分类回归出图、要 σ | **geedl** 或 GEE 自带 RF |
| **数据太大下不动、下载太慢、撞配额或 EECU** | ★ **GeeZip** |
| **要下几十个波段、几十个时相** | ★ **GeeZip**，波段越多越划算 |
| 只下一景、几个波段、小范围 | **别用**，直接下更快 |
| 要逐位精确的存档 | **别用**，它是有损的 |

判据一句话：**下载量是瓶颈才用它。**

判断完先把结论告诉用户一句，再动手。

---

## 只有一个入口

```bash
python run.py 自检 → 取数 → 训练 → 导权重 → 门禁 → 下载 → 解码 → 汇总
python run.py 全部                        上面一整串
python run.py 外推 --roi W S E N          换块没训练过的地验泛化
python run.py 新建 "D:\某目录"             在别处开一条新管线
```

参数原样透传。串跑时各步会忽略不属于自己的参数（单步跑仍然严格报错）。

**没有编号脚本。全部走 `run.py`。**

---

## 六条铁律

### 1. 两道核对都过了才许下载

| 核对 | 验什么 | **验不了什么** |
|---|---|---|
| 一致性 | 同样输入 → 本地和 GEE 同样输出 | **输入本身对不对** |
| 特征指纹 | 两边特征栈算出同样的值 | 模型权重对不对 |

**缺一不可。** 结论是脚本自己判的：末尾打印 `GATE_RESULT PASS/FAIL`，
`下载` 先读这行，FAIL 直接拒绝跑。**不要想办法绕过。**

不过怎么办：
- 一致性不过 → 编码器搬错了（权重精度、激活函数、标准化对不上）
- 指纹不过 → 场景的 `.py` 和 `.js` 两边特征栈漂了

### 2. 「跑通」不等于「对」

GEE 惰性计算，零报错也可能全错。**本项目实际踩到过的：**

| 故障 | 表现 | 谁抓住的 |
|---|---|---|
| `multiply()` 丢 `system:time_start` | `filterDate` 筛不到东西，四个季度全空，下游才报 "no bands" | 报错位置离真 bug 十万八千里 |
| 反向传播分母漏乘 D | 梯度大 D 倍，**照样收敛**，只是收敛到别处 | 只有数值梯度检查 |
| 数组影像全幅物化 | 小范围正常，全幅 `User memory limit exceeded` | 只有真下一次 |

交付前再问一遍：**这些数字讲得通吗？量级对吗？**

### 3. 先小后大，省配额

Debug 用 `--npix 8000 --轮数 60` 和一小块 ROI，通了再放大。

★ **取数只下几万个像元**，这是本工具省配额的关键。
**别退回去下整幅来训练**——那就把 GeeZip 的意义抵消了。

### 4. 改场景改两个文件，改完从 `取数` 重跑

`场景/<name>.py` 和 `场景/<name>.js` 逐行对应。`引擎/` 别动。

### 5. 改了 `引擎/自编码器.py` 的 `反向()` 必须跑梯度检查

```bash
python 检查/梯度检查.py
```

反向传播写错**不会报错**，只表现为"学习率好像不太对"。
本项目就是这么抓到过一次分母漏乘 D 的。

判定轮用 tanh 不用 ReLU——ReLU 在 0 处不可导，预激活贴着 0 的元素
数值梯度没意义，会给假阳性。

### 6. 诚实报告

- 压缩比按**实际传输字节**算，基线用 **uint16**，不是 float32。
  float32 比 uint16 大约 1.9 倍，那部分是"不该用 float32"白送的，
  算进压缩比是自欺欺人。
- 还原误差要和**传感器自身定标精度**比（Landsat ≈ 0.005 反射率）。
- 换地方会不会崩，跑 `run.py 外推` 用数据说话。**退化了照实说。**

---

## ★ 一条关键的工程经验

**别用 `toArray` + `matrixMultiply` 去物化整幅栅格。**

| | 全幅 743×743 @30m |
|---|---|
| 数组影像版 | `User memory limit exceeded` |
| **波段算术版** | **OK，1.19 MB，5 秒** |

原因是 `toArray` 给每个像元造了个数组对象，全幅物化时内存开销巨大。
MLP 的每个隐单元就是个加权和 `u_j = Σ W[j][i]·x_i + b_j`，
用普通波段算术表达即可。

实测两版结果**逐位相同**（全幅最大差 = 0）。
引擎里两版都在：核对用数组版，下载用波段算术版，下载前自动对拍。

---

## 下载会撞的墙（会自动处理，但要知道）

```
Total request size (66245880 bytes) must be less than or equal to 50331648 bytes
Earth Engine memory capacity exceeded
RemoteDisconnected
```

- `getDownloadURL` 有 **48 MB 同步请求上限**，服务端另有内存上限。
  大范围时序数据必然撞 → `下载` 会**自动把边长翻倍重试**，最多 4×4。
- 走代理时 `RemoteDisconnected` 是常态 → 网络级错误自动重试，
  和"服务端说不行"分开处理。

★ **原始数据下不动本身就是结果**，不是失败。
24 波段 float32 要 66 MB 超上限，而隐层 2.44 MB 一次就下来——
报告里要把这一条说出来。

---

## 目录地图

```
配置.txt                单一真源：项目 ID、场景、k、超参、阈值
run.py                  唯一入口
场景/
  landsat年度.py / .js   6 维，先用它跑通
  landsat季度.py / .js   24 维，压缩收益大得多
  _新场景模板.py / .js    换研究对象复制这个
引擎/
  配置.py               读配置、找场景、初始化 EE、参数解析
  自编码器.py            numpy MLP + Adam + 量化感知训练 + PCA 基线
  编码器_ee.py           Python-ee 侧编码器（数组版 + 波段算术版 + 对拍）
  GeeLo定位.py           找 GeeLo（唯一外部依赖）
  步骤/                 七个步骤，一个一个文件
  模板/模板_编码器.js     生成 GEE 脚本用的模板
检查/梯度检查.py          改了反向传播必跑
装GeeLo.js              GeeLo 没装时自己拉一份
产物/                   全部输出（在【当前工作目录】下，不在插件里）
```

---

## 别污染用户的文件夹

- 探针、临时脚本 → **临时目录**
- `产物/` 是全部输出，删掉不影响代码
- `<GeeZip>` 里的东西**别改**，那是插件本体。要在别处干活就 `run.py 新建`

报告要给证据：说"少下了 N 倍"就附实跑输出和关键数值。跑不通就说跑不通。

---

## 凭据

在 `%USERPROFILE%\.config\earthengine\credentials`，**不在仓库里**。

★ **绝对不要打印、贴进对话或写进任何文件**，也不要让用户贴给你。
你只需要知道文件在不在。

项目 ID 用环境变量 `EE_PROJECT`（`认证.js` 会 `setx` 写好），
其次读 `配置.txt` 的 `project`。

---

## 环境事实

- Windows + Python 3.8+ + Node.js 20.19+
- Python 包见 `requirements.txt`，**不需要 torch / tensorflow**
- 前置依赖 **GeeLo**，`配置.txt` 的 `geelo = auto` 会自己找；
  找不到跑 `node 装GeeLo.js`
- PowerShell 5.1 里 `&&` 不可用，用 `;` 或 `if ($?) { ... }`
- 中文 Windows 控制台是 GBK，**加新 print 时别用花哨符号**
- 代理没开的话 `earthengine.googleapis.com` 直接 TIMEOUT，
  自检会报一串失败，根因往往只是这个
